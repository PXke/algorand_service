# Docker buildx local cache
.cache/

# Python
__pycache__/
*.pyc
.venv/

# Flutter
.dart_tool/
build/

# Env & secrets
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/

# Compose prompt eval harness output (local, not for commit)
workers/scripts/eval_compose_output/

# Translation candidate eval harness output (local, not for commit)
workers/scripts/eval_translate_output/

# Prod DB backups pulled locally (local, not for commit)
workers/scratch/article_table_backup/
workers/scratch/full_keyspace_backup/
