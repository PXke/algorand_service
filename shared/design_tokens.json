{
  "$schema-note": [
    "Cross-boundary design tokens: the palette values that are restated OUTSIDE",
    "frontend/src/app.css, in files that cannot read a CSS custom property — a",
    "favicon SVG, the pre-CSS offline page, the SSR <style> block Python emits",
    "the PWA manifest, a <meta name=theme-color>.",
    "",
    "app.css remains the design surface and defines the full palette. Only the",
    "handful of values that must be duplicated live here, so there is one place",
    "to change them and one place to check them against.",
    "",
    "Consumers:",
    "  Python  - shared/algorand_shared/design.py (read at runtime)",
    "  TS      - frontend/src/lib/designTokens.ts (GENERATED, do not edit)",
    "  CSS/SVG/HTML - hand-authored, verified by tools/design/sync_tokens.py",
    "",
    "After editing this file run:  python3 tools/design/sync_tokens.py --write",
    "backend/tests/test_design_tokens.py fails the suite if anything drifts."
  ],
  "tokens": {
    "accent": {
      "light": "#4a5bd0",
      "role": "Brand mark foot rule. The one blue the logo is drawn in."
    },
    "primary": {
      "light": "#3a49ad",
      "role": "Link ink — indigo darkened to clear WCAG AA on --surface."
    },
    "surface": {
      "light": "#eef0f5",
      "role": "Daylight page background. Cool paper, the night surface inverted."
    },
    "panel": {
      "light": "#f7f8fc",
      "role": "Raised paper / glyph fill on the stamp."
    },
    "on-surface": {
      "light": "#14161c",
      "role": "Headline ink and stamp ground."
    },
    "muted": {
      "light": "#5a5e6a",
      "role": "Secondary text: bylines, timestamps, breadcrumbs."
    },
    "subtle": {
      "light": "#767b88",
      "role": "Tertiary marks: breadcrumb separators, hairline glyphs."
    },
    "border": {
      "light": "#d4d7e0",
      "role": "Hairline rules."
    },
    "theme-color": {
      "light": "#eef0f5",
      "dark": "#0f1117",
      "role": "Browser/OS chrome tint. Matches the page, not an inverted bar."
    }
  }
}
