# Deployment target for PXke Algorand Projects (sourced by deploy.sh;
# every value can be overridden from the environment).
TARGET_HOST=5.135.131.229
SERVICE_USER=guillaume
ROOT_USER=root
TARGET_PATH=/home/guillaume/algorand-platform

# Public domains (DNS A records must point at TARGET_HOST)
SITE_DOMAIN=algorand.pxke.me
API_DOMAIN=algorand-api.pxke.me
CERTBOT_EMAIL=px9e@proton.me

# Backend bind port (8080 is taken by algod on this host)
APP_PORT=9080

# Optional: override Python for the shared venv (default: python3.15t → python3.15 → python3)
PYTHON_BIN=python3.15t

# Algorand network for wallet login / explorer links / chain reads
ALGORAND_NETWORK=mainnet

# Wallets allowed to use the admin panel / admin API
FRONTEND_ADMIN_WALLETS=RR4FHUPJE32YGJMS76VPI5MZ2VMCGKTPEGPYORGS3IXJ63U2CVNDUHQLS4

# IndexNow key (public — served as {key}.txt at the site root and used by the
# workers to ping Bing/Yandex/Seznam/Naver on publish). Must match the workers'
# INDEXNOW_KEY default in workers/app/core/config.py.
INDEXNOW_KEY=63e7ffa13f3ca734700ca375c0581b41
