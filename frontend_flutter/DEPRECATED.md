# DEPRECATED — replaced by `frontend/`

This Flutter web client has been replaced by the Vite + Svelte SPA in
[`../frontend/`](../frontend/).

Do not build or deploy from this directory. Kept temporarily for reference;
safe to delete once you no longer need the Dart sources.
